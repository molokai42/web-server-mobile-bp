overwrite: true
schema: "schema.json"
documents: "src/**/*.tsx"
generates:
  src/operation-result-types.ts:
    config:
      noNamespaces: true
    plugins:
      - "typescript-common":
      - "typescript-client":
